import React from 'react'
import PropTypes from 'prop-types'

const Home = ({names}) => {
  return (
    <div>
      
    </div>
  )
}

Home.propTypes = {
 names: PropTypes.string
}

export default Home
